# ========== Перед продакшеном обязательно ==========
# Заполните реальные значения: иначе модерация ЮKassa и доверие пользователей под риском.
# PRIVACY_POLICY_URL=   — ссылка на политику конфиденциальности (ваш домен или telegra.ph)
# TERMS_URL=            — ссылка на оферту / пользовательское соглашение
# CONSENT_PD_URL=       — ссылка на согласие на обработку ПД (152-ФЗ)
# BOT_ABOUT_TEXT=       — реквизиты (ИНН, ОГРН), контакт поддержки (см. docs/MODERATION_EVIDENCE_PACK.md)

# Telegram Bot
BOT_TOKEN=your_bot_token_here
WEBHOOK_HOST=
WEBHOOK_PATH=/webhook
# Секрет для проверки запросов Telegram webhook (если пусто — генерируется при старте; на VPS лучше задать явно)
# WEBHOOK_SECRET_TOKEN=

# Database (async for bot)
# Локально: localhost. На VPS при запуске через Docker Compose используйте хосты db и redis:
#   DATABASE_URL=postgresql+asyncpg://elenabot:YOUR_PASSWORD@db:5432/elenabot
#   REDIS_URL=redis://redis:6379/0
# На VPS без Docker: системный PostgreSQL может слушать порт 5433 — тогда укажите localhost:5433 (см. docs/DEPLOYMENT.md).
# Пароль без спецсимволов или с URL-кодированием (! → %21 и т.д.).
DATABASE_URL=postgresql+asyncpg://user:password@localhost:5432/elenabot
# Sync for Alembic/Celery (optional, auto-derived from DATABASE_URL if not set)
# DATABASE_URL_SYNC=postgresql://user:password@localhost:5432/elenabot

# Redis
REDIS_URL=redis://localhost:6379/0

# YooKassa
YOOKASSA_SHOP_ID=
YOOKASSA_SECRET_KEY=
YOOKASSA_RETURN_URL=https://t.me/YourBot
# Для скриншота модерации (до получения ключей): при ошибке «создать платёж» бот покажет экран с ценой и кнопкой «Оплатить» с этой ссылкой. Задайте, например, https://t.me/YourBot — сделайте скрин, затем уберите переменную или оставьте пустой.
# DEMO_PAYMENT_URL=https://t.me/YourBot
# Опционально для будущей проверки подписи webhook (если появится в API)
# YOOKASSA_WEBHOOK_SECRET=

# Business & payments
FREE_LIMITS_PER_MONTH=5
PAYMENT_PACK_SIZE=10
PAYMENT_PACK_PRICE=100.00
# Cron сброса бесплатных лимитов (по умолчанию 1-е число месяца 00:00 UTC)
# LIMIT_RESET_CRON=0 0 1 * *
# Текст раздела About бота: реквизиты организации и контакт поддержки (обязательно для прода)
# BOT_ABOUT_TEXT=ООО «Пример»\nИНН 1234567890\nОГРН 1234567890123\nПоддержка: @support

# Observability (на VPS рекомендуется)
# SENTRY_DSN=https://xxx@sentry.io/xxx

# Admin Telegram IDs (comma-separated)
ADMIN_TG_IDS=123456789

# OpenRouter (LLM OCR): ключ на https://openrouter.ai
OPENROUTER_API_KEY=your_openrouter_api_key
# Модель с поддержкой зрения: google/gemini-3-flash-preview, openai/gpt-4o-mini и др.
LLM_VISION_MODEL=google/gemini-3-flash-preview
# Макс. сторона картинки в px (уменьшает расход токенов)
LLM_MAX_IMAGE_SIZE=2048
# Таймаут одного запроса к LLM (сек)
LLM_REQUEST_TIMEOUT=90
# DPI при конвертации PDF в картинки; макс. страниц в одном PDF
PDF_OCR_DPI=200
PDF_MAX_PAGES=50
# Windows: путь к папке bin Poppler (скачать https://github.com/oschwartz10612/poppler-windows/releases)
# POPPLER_PATH=C:\path\to\poppler-windows\Library\bin

# 152-ФЗ и модерация: ваши документы (для прода — свои URL, не дефолтные Telegraph)
# PRIVACY_POLICY_URL=
# TERMS_URL=
# CONSENT_PD_URL=

# Docker Compose на VPS: пароль PostgreSQL (задать сильный пароль, не коммитить .env)
# POSTGRES_PASSWORD=your_strong_password
